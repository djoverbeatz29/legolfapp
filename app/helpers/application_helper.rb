module ApplicationHelper

    # def golfer_vars
    #     @scored_rounds = self.rounds.find_by { |ro| ro.score }
    #     @scores = @scored_rounds.map { |ro| ro.score }
    #     @scores_vs_par = @scored_rounds.map { |ro| ro.score - ro.course.par }
    #     {:scored_rounds => @scored_rounds,
    # :scores}
end
